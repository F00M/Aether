// Shared UI constants (kept out of component files so fast-refresh stays intact).

// Protocol accent colors for the route proportion bar. Darkened from the original
// Aurora palette so they stay legible on the light surface.
export const PROTO_COLORS = { v2: '#b8791a', v3: '#0e7490', v4: '#6d45d9' }
